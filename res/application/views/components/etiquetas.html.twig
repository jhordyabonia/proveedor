<div class="row">
    <div class="col-xs-12">
        <h3 class="titulos">Etiquetas</h3>
        <ul class="tags">
        {% for targget in tag %} 
            {% if not (targget == '') %}
                        <li><a href="#" class="tag">{{targget}}</a></li>
            {% endif %}
        {% endfor %}
        </ul>
    </div>
</div>